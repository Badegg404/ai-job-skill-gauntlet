---
name: 程序开发方法论
description: 通用程序开发方法论——方法与思路适用于任何项目，示例来自「AI 面试能力评估系统」（LLM 应用）。核心：LLM 做语义、程序做数据+提示词+兜底、程序兜底不可靠输出、硬规则（次数/顺序/随机性）不交给 LLM 自觉而程序硬约束、PROMPT 统一管理、提示词五要素（角色/任务/上下文/格式/示例）、模块拆分、测试集与回归、影响分析+小步改、数据存多处时清空/加检查要穷举所有点、同类逻辑多路径重复时修一处 grep 所有路径、验证完成而非宣称完成、评审-执行分离（五维度评审）、宁缺毋滥（数量软门槛/不连坐/降级可用）。使用前先读文首「使用说明」，把示例映射到自己的业务，勿照搬具体数据。新项目开发或大改动前先读此文档。
---

# 程序开发方法论 — 经验总结

> **使用说明（重要）**：这是一份**通用程序开发方法论**，不是「出题系统」专用指南。
> 文中的例子来自一个「LLM 出题/考核」项目，但请**只参考其中的方法与思路，把它们映射到你自己的业务**：
>
> | 文中的例子 | 对应的通用场景 |
> |---|---|
> | 出题 | 任何「让 LLM 生成内容」：文案、摘要、代码、数据标注 |
> | 判分 / 语义判定 | 任何「判断内容语义是否等价」：审核、去重、质检 |
> | 能力维度打标签 | 任何「给内容分类 / 打标签」 |
> | 题目校验 | 任何「校验 LLM 输出是否符合 schema」 |
> | 岗位匹配 | 任何「基于多维画像做综合评估 / 推荐」 |
>
> **核心不变**：LLM 做语义，程序做「数据正确 + 提示词正确 + 兜底校验」。换成任何业务，这条边界都成立。

> 来源：AI 岗位面试能力评估系统（Python 后端 + 原生 JS 前端 + LLM 驱动的本地桌面应用）。
> 用法：新项目开发前、大改动前先读一遍；每次开发后回来更新，逐步沉淀成自己的开发思路。

## 一、核心架构原则

### 1.1 LLM 应用的分工边界（最重要的一条）

**LLM 只做「语义/理解」，程序只做「数据正确 + 提示词正确 + 兜底校验」。**

| 交给 LLM | 留在程序 |
|---|---|
| 出题（理解资料、生成题） | 数据结构定义、字段校验 |
| 打标签（语义归类能力维度） | 白名单、默认值兜底 |
| 判分（语义等价判断） | 客观题（选择/判断）直接代码判 |
| 岗位匹配（综合评估） | 分数计算、等级阈值 |

**判断标准**：这件事需要"理解意思"吗？
- 需要理解 → LLM（如"JSON 格式"和"json 格式"算不算同一个答案）
- 不需要理解 → 代码（如"用户选 A 对不对"，代码比对即可）

反面教材：一开始用关键词匹配做语义判分（`detect_ability` 关键词表、填空题子串模糊匹配），结果"语义相近但文字不同"全部误判。**语义识别必须 LLM，代码做关键词匹配只会留下无穷无尽的边界 bug。**

### 1.2 程序兜底 LLM（LLM 输出永远不可靠）

LLM 会：输出格式错、字段缺失、字段越界、返回矛盾内容、被 max_tokens 截断、网络报错。**程序必须对每一份 LLM 输出做校验和兜底，宁可丢弃坏数据，不让坏数据进业务流。**

兜底三件套：
1. **归一化**：把 LLM 的各种写法统一成程序期望的格式（见 3.1）
2. **校验 + 丢弃**：字段缺失/越界/类型错 → 丢弃该条，不影响整体
3. **降级**：LLM 调用失败 → 用程序兜底（完整相等判分、自评按钮、规则兜底）

关键认知：**判断题那个 bug 的本质不是 LLM 错了，是程序没兜底**。LLM 返回 `correctAnswer: "正确"` 而程序只认 `"对"`，责任在程序不在 LLM。

### 1.3 PROMPT 统一管理

所有 LLM 提示词集中到一个文件（如 `prompts.js` / `prompts.py`），不要在业务代码里散落。

好处：
- **统一"严格"原则**：改一处，所有出题/判分/面试都生效
- **白名单、教学法、JSON schema 写一次**，多处复用
- **后续增删改减只动一个文件**

结构建议：
```
SYSTEM（统一角色设定：出题官/面试官/判分官，每个都写"严格、不轻易给高分"）
├── 出题 prompt 构建函数
├── 判分 prompt 构建函数
└── 面试 prompt 构建函数
```

### 1.4 严格提示词原则（贯彻所有 prompt）

- **出题**：考察真实掌握，拒绝背书题，题目要有区分度、能暴露知识盲区
- **打标签**：严格对应白名单维度，宁缺毋滥
- **判分**：不轻易给高分，60 分及格，指出具体短板
- **岗位匹配**：样本不足时保守，不虚高

### 1.5 提示词五要素（写 / 审 prompt 的 checklist）

每个 prompt 都按这五条过一遍，**缺一条输出就容易飘**。口诀：「你是谁、干什么、依据什么、输出成什么样、给个样子」。

| 要素 | 含义 | 本项目示例 |
|---|---|---|
| 1. 角色（Role） | 让模型扮演谁 | `你是资深的 AI 大模型应用开发出题专家` / 各岗位 `systemRole` |
| 2. 任务（Task） | 要它做什么 | `请生成 8 道考核题` / `请判断答案是否语义一致` |
| 3. 上下文（Context） | 给什么材料与约束 | 课程概念/章节/难点 + 岗位知识图谱要点 |
| 4. 输出格式（Format） | 输出长什么样 | 每个 prompt 末尾的 JSON schema + `response_format` |
| 5. 示例（Few-shot） | 给**正例 + 反例** | 判断题「只填对/错」、JSON 结构示例 |

本项目踩过的坑，根因都是缺某一条：
- 导入出题原来**没设角色**（开头直接「请根据下面生成考核题」）→ 补角色后更稳定
- 判断题 LLM 返回「正确」而程序只认「对」→ **缺示例/约束**，没告诉它只能填「对/错」
- 填空题用子串模糊匹配误判 → **任务边界错**（该 LLM 做语义判定的，交给了代码）

经验：**角色和格式最容易漏**。角色决定「口吻与深浅」，格式决定「能否被程序解析」；这两条写清楚，输出质量提升最明显。

**示例要「正例 + 反例」成对给**：正例教它「照这样做」，反例教它「别这样做、边界在哪」，比只给正例更能约束 LLM 不越界。典型反例约束：`correctAnswer 只能填「对」或「错」，不要填「正确 / true / √」`；`只输出 JSON，不得输出任何额外文字、注释或 markdown`。

### 1.6 硬规则（次数 / 顺序 / 随机性）不能靠 LLM「自觉」，必须程序兜底

LLM **读得懂** prompt 里的规则，但**不会可靠地「每次都遵守」**规则——尤其当规则要求它"每次都必须这样做"时（追问 N 次、随机、不重复），它经常"换个说法就绕过"。

本项目连续三次踩坑（根因都是「把硬规则交给 LLM 自觉」）：

| 需求 | 错误做法（交给 LLM） | 现象 | 正确做法（程序硬约束） |
|---|---|---|---|
| 每题追问 ≥3 次 | prompt 写「每题追问 ≥3 次」 | 候选人答「不清楚」（而非「不知道」）就跳过 | 前端计数强制追问，LLM 只负责**生成追问文案** |
| 出题顺序随机 | prompt 写「随机出题」 | LLM 还是按参考题固定顺序出 | 前端**打乱参考题顺序**再喂 |
| 识别「答不上来」 | 靠 LLM 判断 | 判断不稳定、换说法就漏 | 前端**正则/规则**判断（短回答 + 弱词） |

**结论**：凡是「必须保证」的约束——次数、顺序、随机性、白名单、去重、兜底——一律程序硬编码；LLM 只负责「生成内容」（文案/题目/点评），**不负责「决定流程」**。

判断标准：这条规则是"必须每次都成立"的吗？
- **必须每次成立**（≥3 次、随机、不重复、白名单）→ 程序硬约束
- **允许灵活**（措辞、语气、深度、是否追问的具体问法）→ 交给 LLM

这也解释了 1.1 的分工边界为什么不止是「语义 vs 数据」：**流程控制、节奏、随机性这些「硬规则」同样属于程序侧，不属于 LLM 侧**。

## 二、开发流程方法论

### 2.1 影响分析 + 小步改（大改动的正确姿势）

**改之前先列影响面，再逐一修改，每步验证。**

三步法：
1. **改之前 —— 列影响面**：先摸清"谁调用了它、它依赖谁"。grep 所有调用点、确认依赖链，再动手。
2. **改之中 —— 一步只动一件事**：拆模块先拆无依赖的，再拆依赖它的；改完一步立刻验证（`node --check` / `python -c "import x"`），而不是一口气全改完再查。
3. **改之后 —— 回归验证**：跑测试集 + smoke 主流程。

价值：出错时排查范围从"整个项目"缩小到"刚改的 10 行"。

### 2.2 测试集（每次大改动后跑一遍）

**先建测试网，再做重构**——测试就是重构的安全网。

- 后端纯函数用 `unittest`，前端纯函数用 Node `vm` + mock 浏览器全局
- 覆盖**历史 bug**（防止回退）+ **核心逻辑**（等级阈值、收缩公式）
- 一键脚本 `run_tests.sh` 跑全部

教训：测试集建好后**当场抓到一个真 bug**（`fixTruncatedJSON` 把 `{` 也补成 `]`）——没有测试，这种 bug 要等用户点开 app 才发现。

### 2.3 模块拆分原则

**该拆的拆干净，不该拆的别硬拆。过度拆分本身也是坏味道。**

- **拆**：低耦合的纯函数（判分、画像、工具函数）、依赖链清晰的层（utils → storage → pipeline → http）
- **不拆**：和 DOM、全局 state、导航耦合极深的代码（硬拆会引入大量跨模块调用和加载顺序问题）

判据：这个函数能不能"独立理解"？能 → 拆；离开上下文就读不懂 → 别拆。

### 2.4 评审-执行分离循环（双 agent 或自查）

大改动时，执行与评审分离（自己执行就自己按维度评审，或交给第二双眼睛）：
- **评审五维度**：① 逻辑正确性（分支/循环/停止条件，找死循环空转）② 数据正确性（字段/去重/统计口径）③ 边界与竞态（限流/空数据/并发）④ 与现有机制一致性（其他入口是否同步改）⑤ 回归风险（相邻模块是否被误伤）
- **产品决策变更必须用户拍板**：执行方擅自改目标值/规则（如题量 16+10 → 14+8）即使技术上合理，也可能违背用户既定需求——标注「需确认」，不默认通过
- **锁不变量用测试**：把「必须永远成立」的约束抽成纯函数（如 alignTheoryCount），用边界值断言锁死（0/1/3/4→4、5/6/7/8→8、17→20）——比「断言调用点传入值」更根本
## 三、LLM 集成的具体坑

### 3.1 输出格式不稳定 → 归一化 + 多级解析

- **判断题**：`correctAnswer` 可能是「对/正确/true/True/是/√/错/错误/false/False/否/×」，程序统一归一化为「对/错」
- **JSON 解析**：先 `JSON.parse`；失败用正则提取 `{...}`；再失败（被截断）补全括号
- **`response_format: json_object`** 中转站可能不支持 → 400 时去掉该字段重试一次

### 3.2 截断 JSON 补全（用栈，别数括号）

`max_tokens` 截断 JSON 时，补全要**用栈记录括号类型，逆序补正确闭合符**。

❌ 错误做法：`"]".repeat(未闭合数)` —— 把 `{` 也补成 `]`
✅ 正确做法：栈里 push 开括号，pop 时 `{`→`}`、`[`→`]`

### 3.3 打标签白名单（LLM 会注入任意字符串）

LLM 返回的 `ability` 可能不在预期维度里，程序必须白名单兜底：

```js
if (!ABILITIES.includes(q.ability)) q.ability = "提示词工程";
```

### 3.4 教学法融入出题提示词

让题目考察"真理解"而非"背书"，可融入：费曼技巧（去术语化解释）、苏格拉底追问（层层深入）、布鲁姆分类（记忆→创造）、主动回忆（挖空补全）、类比迁移（生活类比）、反例与边界（何时失效）。

### 3.5 调用外部接口：HTTP 200 不代表成功

`fetch` 的 `r.ok` 只表示「网络层成功」（状态码 200-299），不代表「业务层成功」。有些地址/中转站对错误请求也返回 200，body 里却是错误信息或网页。判断成败要**再解析响应内容**：

- body 有 `error` 字段 → 失败
- body 缺 `choices`（聊天接口）→ 失败
- body 不是 JSON（是网页）→ 失败

通用：任何"调外部接口判断成败"，都要看响应内容，不能只看状态码。

### 3.6 异步请求的竞态守卫（LLM 是慢请求，最容易乱序）

LLM 调用是"慢异步"：网络延迟 + 模型推理时间，**先发的请求可能后返回**。只要用户在等待期间可能发起另一个同类操作（切考核模式、重新出题、再次导入），旧请求的响应晚到就会覆盖新状态。

解法：**发起时取号，返回时校验归属，非最新号直接丢弃**。

```js
const seq = ++examSeq;         // 发起时取号
const res = await fetch(...);
if (seq !== examSeq) return;   // 返回时校验：不是最新请求，丢弃
```

真实案例：快速切换考核模式时，旧试卷生成请求晚于新请求返回，覆盖了新试卷（`examSeq` 竞态守卫，注释 `旧请求返回不覆盖新试卷`）。

判断标准：任何"发出去才知道多久回来"的异步，只要用户可能在等待期间触发另一个同类操作，就必须做响应归属校验。


### 3.6 补充：异步长流程防重入锁（importBusy）

除了「响应归属校验」（上面），**流程级还要防重入**：导入/生成这类几十秒的长流程，用户可能再次点击触发——两个流程并发会互相覆盖状态/写盘。解法：模块级 `busy` 锁——进入置 true，再次触发直接提示「正在处理中」并忽略；**成功/失败路径都要复位**（finally 或每个出口），否则锁死后续操作。

### 3.6 补充 2：重置/清空类操作的竞态根治（时间戳墓碑）

「fire-and-forget 写盘 + 之后清空」是经典竞态：**清空动作之前已发出的旧写盘请求可能晚于清空到达，把旧数据写回（复活）**。典型症状：用户重置全部数据后，旧数据（能力画像/记录/徽章）还在。

只 await 清空请求不够——旧请求是清空**之前**发出的，await 拦不住「已发出的迟到请求」。根治要用**服务端时间戳墓碑**：

1. 清空接口（reset-all）删除数据后写一个 `reset-ts` 时间戳文件（墓碑）
2. 写盘接口（profile-save）的请求体携带**保存发起时刻 `clientTs`**（前端 fetch 时打 `Date.now()`）
3. 服务端校验：`clientTs < reset-ts` → 该请求是清空前挂起的旧请求，**丢弃不写盘**（返回 ok 但标记 stale）；缺 clientTs（老客户端）不拦截，保持兼容
4. 前端清空流程顺序改为：**先重置内存状态 → 再清空服务端 → 再同步空状态落盘 → 再刷新**——保证清空后写回的一定是空数据

判断标准：任何「写盘请求是异步发出、之后可能被清空」的场景（重置/删除/覆盖），都要防「旧请求晚到复活旧数据」——不能只靠前端 await，服务端要能区分请求新旧。

### 3.7 宁缺毋滥：LLM 数量约束分级（精确配比遵从度低）

让 LLM 生成内容时，**别要求「恰好 N 道 X 型 + M 道 Y 型」的精确配比**——LLM 对精确数量/配比的遵从度不稳定，硬性要求会系统性失败（反复重试 → 超时 → 整体失败）。

正确做法：
- **硬性底线**：总量必须达到（如「至少 10 道」）
- **软性期望**：结构配比「尽量」（如「尽量含 2 道写代码题，不足用客观题补」），不足不阻塞，由其他机制兜底（考核时动态生成）
- **没有素材就不硬凑**：让 LLM 基于不存在的素材（如无代码文件却要求代码题）只会瞎编或返回空——**宁缺毋滥：没有素材就跳过该类题**，而不是硬凑低质量内容

真实案例：要求 LLM「必须恰好 8 道 code_choice + 2 道 llm_code」，LLM 经常只给 0-1 道 llm_code → 触发补足循环空转 5 次 → 导入失败删目录。改成「总量 10 道硬性，llm_code 尽量不强制」后，LLM 稳定产出 10 道客观题，导入恢复。

### 3.7 补充：宁缺毋滥深化（数量软门槛 + 不连坐 + 降级可用）

数量约束的最终形态（多次迭代后）：
- **最低可用量是软门槛**：单批 ≥ 底线（如 4/3）就收，不用等凑满理想量（16/10）——理想量由**累积补足**（多轮小批量）达成，而不是靠单次「凑满或失败」
- **有多少收多少（lastNonEmpty）**：LLM 给不足时**返回已收到的**（不 throw）——主流程补足循环继续累积；仅「从未出过题」（空响应重试耗尽/格式全坏）才真失败
- **服务限流不连坐**：LLM 空响应（限流）导致某类题不足时，**不要整体失败**（否则连累已成功的另一类题全丢）——已成功部分入库 + 不足部分软提示（按钮置灰 + 补出题入口）
- **降级可用**：不足时导入仍算成功（目录 + 部分题 + 奖励照发），考核按钮置灰提示补足——而不是失败+删数据
- **自洽公式优于对齐**：题型分配用公式 `判断=填空=⌊N/4⌋, 选择=N−2×⌊N/4⌋`（任意 N 自洽）——比「先对齐到 4 的倍数再分配」更根本（少一层约束，少一类 bug）

### 3.8 LLM 输出统一约束（Agently custom() 机制）

「要求 LLM 输出 JSON」不保证它遵守。**程序级约束**：
1. 每个请求尾部附加**标准格式约束块**：顶层结构、字段名、禁止 markdown 代码块、数量要求
2. **解析/校验失败 → 把错误反馈给 LLM 重新输出**（最多 3 次）——比单纯强调「严格」有效得多（Agently custom() 机制）
3. **空响应软处理**：LLM 返回空内容 = 服务限流/异常——重试 ≤2 次后**返回已收到的（lastNonEmpty）或空数组，不 throw**（否则连累同批次已成功的另一类题全丢，见 3.7「不连坐」）；由主流程补足循环 + 软提示兜底——别空转，也别连坐
4. **所有 LLM 调用统一走一个约束函数**（单一标准），不各自为政

真实案例：LLM 把 JSON 包在 markdown 代码块里、或嵌套/截断，解析器全解析出 0 题。加统一约束块 + 失败反馈重出 + 剥代码块/递归找数组的容错解析后，导入从「稳定失败」变为「稳定成功」。

### 3.9 诊断优先：LLM 交互问题先加日志，不盲猜

LLM 交互出问题，**先加全链路日志再修**：请求开始（模型/参数）、重试（状态码）、失败（响应体前 1500 字）、解析数量、最终错误——落盘。

真实案例：用户报「导入失败」，凭直觉猜是网络/429，查日志实据发现**请求成功、LLM 返回了 10KB 内容、但解析器解析出 0 题**——根因是 JSON 格式不统一。没有日志只能盲猜，有日志一击命中。

**日志是 LLM 应用的第一公民**——LLM 输出不可预测、调用是慢异步，出了问题无法断点调试，**日志是唯一的回放手段**。设计要点：

1. **全链路事件化**：start（模型/参数/提示词长度）→ retry（状态码/次数）→ ok（解析数量/响应体长度）→ fail（错误消息 + 响应体快照前 1500 字）——每个关键节点一条日志
2. **响应体快照**：LLM 返回了什么必须能回看（尤其解析失败时，原始内容就是根因证据）——只记长度不够，要记内容头
3. **落盘到服务端**：前端浏览器没有持久日志，把诊断 POST 到后端落盘（用户报告问题 → 直接读日志文件定位）
4. **别静默吞错误**：`catch(() => {})` 是调试杀手——任何失败路径至少要记一条日志（tag + message），不能无声无息
5. **日志即产品**：报错信息从日志读到的真实原因生成（HTTP 状态/响应体/解析数量），用户看到的具体、可行动

### 3.9 补充：日志系统的落地形态（前端事件 + 后端文件 + 查看入口）

原则之上，一套「零新依赖」的日志系统怎么搭（参考本项目落地）：

1. **三层分工**：
   - **前端事件层**：浏览器侧 Logger 单例（info/warn/error + 批量上报 ≤50ms 合并一次 POST），功能打点（start/done/fail）+ **全局兜底**（`window.onerror` + `unhandledrejection`——JS 运行时错误不静默）
   - **后端文件层**：stdlib `logging` + `RotatingFileHandler`（单文件 2MB × 5 份自动轮转）——全局 app.log（启动/请求耗时/未捕获 traceback）+ 每用户 activity.log（业务事件）；**HTTP 分发处统一包装**（method/path/耗时，>2s 记 warn，handler 抛异常自动记 traceback 并返回 500），比在每个 handler 里打点更全更省
   - **查看入口层**：GUI「诊断日志」页（只读接口 + 按级别/tag/session 过滤 + 导出 + 清空）——用户报 bug 直接复制日志，不开终端
2. **JSONL 单行格式**：`{at, level, session, tag, msg, payload}`——`session` 是**流程级会话 id**（一次导入/考核/面试一个），日志按 session 聚合就是一条完整链路（start→retry→ok→fail）
3. **静默吞错审计**：改造时 grep 所有 `except: pass` / `catch(() => {})`，逐个决定补 warn 还是保留（读取回退空值是正常设计，但要记 warn 提示数据损坏）
4. **隐私与体积**：API key 永不入日志；LLM 响应快照截断（内容头 1500/800 字）；prompt 只记长度不记全文
5. **兼容渐进**：旧上报接口保留（前端旧调用点零改动），新日志与旧日志文件并存，诊断页可读两者

### 3.10 失败保留 + 自救入口（不删用户数据）

LLM 生成失败时，**不要自动删除用户数据**（已解析的资料骨架/文件清单）。正确姿势：
- 保留 + 明确失败原因提示
- 提供**自救入口**（如「补出题」按缺失补齐：理论缺补理论、实战缺补实战）
- 半成品状态可视化标识（如「⚠️ XX 缺失，点补出题」）
- 仅用户手动操作才删除

### 3.11 事件驱动流程 → StateGraph（LangGraph 式）

多阶段交互流程（面试官：出题→作答→评估→追问→换题→结束），事件驱动 + if/else 路由会散落各处、状态难维护。重构为**轻量状态图**（不引入框架，自研 ~15 行执行器）：
- **节点**：每阶段一个函数（出题/记录/评估/决策/追问/推进/结束）
- **条件边**：路由规则集中（答好→追问、答差→换题、连续弱答→提前终止、轮尽→结束）
- **共享状态**：显式流转，天然防竞态
- **human-in-the-loop**：等用户输入的节点用 WAIT 中断，作答后从该节点恢复
- **入口保留**：对外函数名不变，行为等价，逐步迁移

### 3.12 多语言工具链转义坑（TS → Python → JS 逐层转义）

在代码生成工具（TS 模板字符串/数组 → Python 脚本 → 写入 JS/Python 文件）链条中，反斜杠 n、插值表达式、双引号**每层都会被转义或插值**：
- TS 模板字符串里的插值表达式会被 TS 求值 → 写 bash 命令要用 $(...) 命令替换内联或转义
- 反斜杠 n 每过一层字符串字面量就变一次（TS 双反斜杠 n → 文件单反斜杠 n → 求值换行）
- 数组元素里嵌套单引号直接语法错

安全做法：**base64 管道**（echo <b64> | base64 -d > file）、**chr() 行级构造**、**read + edit 精确替换**——别信嵌套转义链。


## 四、具体 bug 案例库（现象 → 根因 → 解法）

| 现象 | 根因 | 解法 |
|---|---|---|
| 判断题正确答案都是第一个 | LLM `correctAnswer` 格式不统一 | 归一化对/错 + prompt 明确 |
| 点击下一题无反应 | 按钮 onclick 状态管理错乱 | 已答题显示"下一题"，未答显示"确认答案" |
| 第 N 课理论考核灰 | 章节识别正则只认「第 N 章」 | 正则扩展到「章/课/节」+ emoji 前缀用 `re.search` |
| 能力分数上来就 100% | 收缩公式参数太松 | 调 BASE/K（如 `(sum+BASE*10)/(count+K)`） |
| 雷达图文字重叠 | canvas 上画文字挤 | 改 HTML grid legend，canvas 只画图形 |
| 多个 toast 重叠 | 每次新建 DOM 节点 | 单例 toast + 复用节点 |
| LLM 设置保存后 UI 无变化 | 保存后没重渲染 | 保存后调 `showSettings()` 重渲染 |
| 错误 API 也提示"连接成功" | 只判断 `r.ok` 状态码，没看 body | 解析 body 验证 `choices`/`error`（见 3.5） |
| 没配 LLM 也能导入并"生成题目" | 规则出题（代码文件）绕过 LLM 拦截 | 所有出题入口都加 LLM 检查，grep 穷举触发点 |
| 重置后首页还显示旧课程、昵称没清 | 只清浏览器 localStorage，漏了本地文件（course.json/profile.json/dirs） | 新增 reset-all 接口，前后端数据都清 |
| 重置后徽章"复活" | state 同步到 profile 是 fire-and-forget，重启读回旧数据 | 清空时 `await` 确保落盘 |
| 欢迎弹窗重置后反复出现 | 重置逻辑又清了 llmGuided 标记（回归） | 重写时对照已修约束，别把刚修的改回去 |
| 不同课程出同一套题 | LLM 动态出题只喂了「概念+章节」，没喂课程特有的「代码文件」，只能出泛化题 | 把课程特有素材（代码/数据）也喂给 LLM，并强制「结合具体内容、禁止泛化」 |
| 实战题被统计成理论题 | 统计按 `type` 分维度，但「场景选择题」的 `type` 是 `choice`、`dimension` 才是 `practical` | 统计/筛选统一按 `dimension` 字段，不按 `type`（`type` 和 `dimension` 是两回事） |
| 修 BUG 只修一条路径 | 同类逻辑在「面试 / 理论 / 实战」多条路径重复，只改了面试路径的白名单过滤 | 修一处后 grep 所有同类路径，确保对称（见 7 检查清单） |
| 面试考一次 ≈ 理论考两次（画像被面试主导） | 面试维度得分 ×2 计入能力画像，理论考核是 ×1，量纲不一致 | 同一张画像表所有来源统一口径（都按 count+1 累加），权重差异必须显式、审慎 |
| 升级当次徽章不出现，下次才补发 | `state.level` 在徽章检查**之后**才同步 | 依赖某字段的判断，必须在该字段更新**之后**执行（先同步，再消费） |
| 导入成功提示题数不对（显示 0 或笔记题数） | 统计用了 data.dir.course（后端挂库前）而非 data.course.quiz（前端挂库后，LLM 题已入） | 统计/提示要用「对时刻」的数据源——区分挂库前/后 |
| 导入成功路径必抛 ReferenceError（theoryWarn） | const 声明在内层 try 块，success 流程在外层引用（块级作用域失效） | 跨块使用的变量声明提升到外层（let 初始化空值），内层只赋值 |
| LLM 首轮 16 道总被截断 | maxTokens 变量定义了但没传进请求（死变量，一直走默认值） | 定义了的参数要确认真的传入调用点（grep 传参） |
| 导入中再次点击导入 → 并发竞态 | 长异步流程无防重入锁 | 模块级 busy 锁 + 成功/失败路径都复位（见 3.6 补充） |
| 笔记自带测验题混入题数统计（39 题 vs 26+5） | 解析器把笔记内嵌测验块也入库（source current） | 入库来源要分清：LLM 生成 vs 资料自带 vs 服务端生成——统计口径与 UI 展示一致 |

## 五、前端经验

- **缓存**：静态资源用 `?v=文件mtime` 做 cache busting（Safari 缓存顽固）
- **状态持久化**：用 localStorage，但记住"清 localStorage 才能回到新用户状态"
- **浏览器直连 LLM**：API Key 存 localStorage，浏览器直连 API，key 不出浏览器、不经服务器（隐私更好，也省后端转发）
- **单例模式**：toast、modal 等一次性 UI 用单例，避免重复创建叠加
- **canvas vs HTML**：图形用 canvas，文字标注用 HTML（避免文字重叠、更好响应式）

## 六、数据与迁移

- **数据存用户主目录**（`~/.xxx/`），不存项目目录（PyInstaller 打包后项目目录不可写）
- **数据格式变更**：改了标签/字段结构，**旧数据要重新导入**（标签在导入时打上），文档要提醒用户
- **版本字段**：数据结构里留 `schemaVersion`，便于将来迁移判断
- **数据常存多处**：浏览器 localStorage（昵称、Key、状态）+ 本地文件系统（profile、course、目录索引）。**做"清空/重置"前先 grep 所有存储点、逐个清**，别只清最明显的那一处——否则就会出现"重置后首页还显示旧课程、昵称没清"。
- **区分「存储数据」和「派生数据」**：徽章不是单独存储，是从 state 计算（`BADGES.filter(b => b.check(state))`）。清 state 的派生字段 = 清徽章，不需要也没法单独清"徽章表"。
- **fire-and-forget 的异步不保证落盘**：`fetch(...).catch(()=>{})` 这种"发了就不管"的同步，可能在进程退出前没写完。**清空/保存这类"必须生效"的操作要 `await`**，否则下次启动读回旧数据（徽章"复活"就是这个）。

## 七、大改动前检查清单

改代码前，逐项过一遍：

- [ ] 谁调用了这个函数/模块？（grep 调用点）
- [ ] 它依赖谁？（确认依赖链，避免循环依赖）
- [ ] 数据格式会不会变？变了波及谁？（前端读/存储读/老数据兼容）
- [ ] 有测试覆盖这块吗？没有先补测试
- [ ] 改完能不能立刻验证？（node --check / import / smoke）
- [ ] 文案有没有跟着改？（架构变了，UI 提示、注释、日志里的旧说法都要同步）
- [ ] 加"拦截/检查"？先 grep **所有会触发该行为的调用点**（主入口 + 旁路），逐个确认——"导入"拦了，别忘了"追加文件"这种旁路
- [ ] 做"清空/重置"？先 grep **所有存储点**（localStorage + 文件系统），逐个清
- [ ] 重写一段逻辑？对照**之前修过的约束清单**，别把刚修好的又改回去（回归）
- [ ] 同类逻辑在多条路径重复出现？（如「面试 / 理论 / 实战」各写一份校验）修一处后 grep **所有同类路径**，确保对称——最容易漏的就是"只改了主路径，旁路还是旧的"
- [ ] 宣称"完成/闭环/彻底"前，**实际验证过吗**？（curl 实测接口返回、find 查磁盘、点一遍主流程）

## 八、警惕「想当然宣称完成」

本方法论最贵的教训，是连续两次被追问"真的闭环了吗""真的彻底了吗"——而两次都确实没做干净。

**嘴上说"完成/闭环/彻底"≠ 真的完成。** 凡是需要"穷举"的改动（清空、加检查、列影响面），最容易漏的就是**旁路和不显眼的那一处**。宣称完成前，用可验证的动作代替直觉：

- "闭环"→ 把每个触发点 grep 出来，逐个打勾
- "彻底"→ curl 实测每个数据接口返回空、find 查磁盘确认文件已删
- "没 bug"→ 跑测试集，而不是"我改完了"

判断标准：**如果你只能说"我觉得应该好了"，说明还没验证；能说"我 curl 看到返回空了 / find 看到文件删了"，才算完成。**

## 九、持续更新说明

每次开发后，把新踩的坑、新的经验补进来，逐步形成自己的开发思路。重点是**把「现象→根因→解法」记下来**，而不是只记"我修了 bug"。
