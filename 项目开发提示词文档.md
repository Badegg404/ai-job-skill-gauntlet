# 项目开发提示词文档（AI 岗位能力试炼 · 实战沉淀）

> **用途**：以后开发任何新项目（尤其是 LLM 应用 / 本地工具 / 前后端一体的小型产品）时，把本文档**整体或核心段落直接喂给 AI**，让 AI 按本项目验证过的架构原则、提示词工程、工程实践与开发流程工作。
> **来源**：本项目「AI 岗位能力试炼」（Python stdlib 后端 + 原生 JS 前端 + 浏览器直连 LLM 的本地桌面应用）从 v2 到现在的全部实战经验，含踩坑、定案与迭代史。
> **用法**：① 新项目直接复制【一、核心可复用提示词】整段；② 开发中遇到具体问题查【三、设计依据】与【四、附件速查】；③ 每次开发结束把新坑沉淀回本文档（保持"现象→根因→通用解法"口吻）。

---

## 一、核心可复用提示词（新项目直接粘贴这段）

```
你是一位资深全栈工程师，请从零开发一个本地优先的小型应用「<项目名>」。
技术栈铁律（与「AI 岗位能力试炼」同款，零第三方依赖）：
- 后端：Python 标准库（ThreadingHTTPServer 或同类零框架），数据落盘 JSON 文件，不用数据库/不用外部依赖
- 前端：原生 HTML/CSS/JS（无框架、无构建工具），浏览器直连用户配置的 OpenAI 兼容 LLM（Key 不出浏览器）
- 打包：PyInstaller 成桌面应用（可选）；所有配置存用户主目录私有目录

【核心功能】<在这里填写本项目的功能清单>
（本项目示例：导入学习资料→LLM 解析出章节目录→LLM 基于资料自动出题（理论客观题+实战代码题）→三大考核（理论客观题程序判分/实战题 LLM 语义判分/面试官动态出题追问评分）→能力画像雷达图+岗位匹配+游戏化（XP/等级/徽章）→全功能日志系统）

【架构铁律（最重要，必须逐条遵守）】
1. LLM 只做语义，程序做数据正确+提示词正确+兜底校验：
   - 需要"理解意思"的（出题/判分/打标签/综合评估）→ LLM
   - 不需要理解的（客观题对错比对、次数/顺序/随机性/白名单/去重/兜底）→ 程序硬编码，绝不交给 LLM"自觉"
2. 程序兜底 LLM：LLM 输出永远不可靠（格式错/字段缺/被截断/空响应/网络错）。归一化→校验丢弃→降级，宁可丢弃坏数据不让坏数据进业务流
3. PROMPT 统一管理：所有提示词集中到一个文件（如 prompts.js），不散落在业务代码；"严格"原则写一次多处生效
4. 提示词五要素：每个 prompt 都要有 ①角色（你是谁）②任务（干什么）③上下文（依据什么）④输出格式（输出成什么样）⑤示例（给个样子，正例+反例成对）
5. 硬规则程序化：次数（如追问≥3 次）、顺序（随机出题）、白名单、去重、兜底——凡是"必须每次都成立"的约束一律程序硬编码；LLM 只负责生成内容文案，不负责决定流程
6. LLM 数量要求宁缺毋滥：不要求"恰好 N 道 X 型+M 道 Y 型"（遵从度低）；总量给硬底线、结构配比用"尽量"；没有素材就不硬凑；不足降级可用+补足入口，服务限流不连坐（已成功的不丢）

【LLM 输出统一约束机制（所有 LLM 调用必须走同一个约束函数）】
- 请求尾部附加标准格式约束块：顶层结构、字段名、禁止 markdown 代码块、数量要求
- 解析/校验失败 → 把校验错误反馈给 LLM 重新输出（最多 3 次），比单纯强调"严格"有效
- 空响应（限流）= 重试 ≤2 次后返回已收到的（有多少收多少），不 throw、不空转、不连坐；仅"从未出过题"才真失败
- 解析容错：先 JSON.parse → 失败剥 markdown 代码块 → 再失败截取 {…} → 被截断用栈补全括号（{→}、[→]）
- HTTP 200 不代表成功：解析 body 验证 choices 存在 / error 字段 / 是否 JSON

【工程防线】
- 异步竞态守卫：发请求时取号（seq++），返回时校验序号，非最新丢弃——LLM 是慢请求，先发的可能后返回
- 长流程防重入锁：模块级 busy 锁，进入置 true，再次触发提示并忽略；成功/失败路径都复位
- 数据源区分：统计/提示用"对时刻"的数据源（如挂库后 vs 挂库前），别用错
- 重置/清空类操作防"旧数据复活"：fire-and-forget 写盘请求可能晚于清空到达 → 服务端写 reset-ts 时间戳墓碑，写盘请求带 clientTs，clientTs < reset-ts 的丢弃

【日志系统（第一公民）】
- 日志是 LLM 应用唯一的回放手段：全链路事件化（start/retry/ok/fail 每个关键节点一条）+ 响应体快照（解析失败记原始内容头 1500 字）
- 前端浏览器没有持久日志 → 前端 Logger（批量上报 ≤50ms 合并）+ window.onerror/unhandledrejection 全局兜底 + 功能打点（start/done/fail）
- 后端 stdlib logging + RotatingFileHandler（2MB×5 轮转）：全局 app.log（启动/请求耗时/未捕获 traceback）+ 每用户 activity.log（业务事件 JSONL）
- JSONL 单行格式 {at, level, session, tag, msg, payload}；sessionId 关联一次流程（导入/考核/面试一条链）
- 别静默吞错误：任何失败路径至少一条 warn/error；except:pass / catch(()=>{}) 逐个审计
- 日志即产品：用户看到的报错信息从日志真实原因生成
- GUI 诊断中心：查看/过滤（级别/tag/session）/导出/清空
- 隐私：API key 永不入日志；快照截断；prompt 只记长度

【开发流程】
- 先建测试网再改代码：后端纯函数 unittest，前端纯函数 Node vm + mock 浏览器全局；覆盖历史 bug + 核心逻辑（阈值/公式不变量）；一键脚本跑全部
- 小步改：改之前列影响面（grep 所有调用点），一次只动一件事，改完立即验证（node --check / python -c import）
- 修一处 bug 后 grep 所有同类路径确保对称（面试/理论/实战多条路径经常同逻辑）
- 产品规则变更（题量/阈值/流程）必须用户拍板，AI 不擅自决定
- 大改动后按五维度评审：①逻辑正确性（分支/循环/停止条件）②数据正确性（字段/去重/统计口径）③边界与竞态（限流/空数据/并发）④一致性（其他入口是否同步改）⑤回归风险
- 大项目用会话交接文档（HANDOFF）：开场让 AI 先读"项目现状/架构关键点/已知坑/协作模式"，避免每次会话从零理解

请先给出项目文件结构规划与核心模块设计，再按 阶段1（数据层+解析）→ 阶段2（核心业务）→ 阶段3（LLM 集成）→ 阶段4（日志+打磨）逐步实现，每阶段可运行、可测试、可回归。
```

---

## 二、这份提示词为什么这么写（设计依据，可裁剪）

| 提示词条目 | 背后的血泪教训 |
|---|---|
| 技术栈零依赖 | 本项目 Python stdlib + 原生 JS，打包体积小、无版本地狱；LLM 中转站/网络问题已经够多，别再引入构建/依赖变量 |
| LLM 只做语义 | 早期用关键词匹配做语义判分（detect_ability 关键词表、填空题子串模糊匹配）→ "语义相近但文字不同"全误判。语义识别必须 LLM，关键词匹配只会留下无穷边界 bug |
| 程序兜底 | 判断题 LLM 返回「正确」而程序只认「对」——责任在程序不在 LLM。归一化（对/错）、白名单（ability 注入任意字符串→回退默认）、降级（自评按钮/规则兜底）三件套 |
| 提示词五要素 | 踩坑根因几乎都是缺某一条：导入出题没设角色→不稳定；判断题没给正反例→输出「正确」；填空题交给代码模糊匹配→任务边界错。角色与格式最容易漏 |
| 硬规则程序化 | 连续三次踩坑：prompt 写"每题追问≥3次"→候选人答「不清楚」就跳过；prompt 写"随机出题"→仍按参考题固定顺序；靠 LLM 判断"答不上来"→不稳定。全部改前端正则/计数/洗牌硬约束 |
| 宁缺毋滥 | 要求"必须恰好 8 道 code_choice + 2 道 llm_code"→LLM 只给 0-1 道→补足循环空转 5 次→导入失败。改"总量硬底线+配比尽量"后稳定产出。数量软门槛（≥底线就收）+ 不连坐 + 降级可用 + 自洽公式（⌊N/4⌋） |
| 统一约束函数 | 各调用点各自解析→有的剥代码块有的不剥，输出格式五花八门。统一 llmJSON（格式约束块+失败反馈重出+空响应软处理）后导入从"稳定失败"变"稳定成功" |
| 竞态守卫+防重入 | 快速切换考核模式→旧请求晚到覆盖新试卷（examSeq）；导入中再点导入→两个流程并发覆盖状态（importBusy）。LLM 慢异步必有乱序问题 |
| reset-ts 墓碑 | 用户实测：重置全部数据后能力画像数据"复活"——重置前挂起的 profile-save 晚到写回旧数据。只 await 清空不够，服务端要能区分请求新旧 |
| 日志第一公民 | 用户报"导入失败"，凭直觉猜网络，查日志实据：请求成功、LLM 返回 10KB、解析器解析出 0 题——根因是 JSON 格式不统一。没日志只能盲猜 |
| 测试网先行 | 测试建好当场抓到 fixTruncatedJSON 把 { 补成 ] 的真 bug——没测试要等用户点开 app 才发现 |
| 用户拍板 | 执行方擅自把题量 16+10 改成 14+8，技术上合理但违背用户既定需求——产品决策变更必须标注「需确认」 |
| HANDOFF 交接 | 新会话开场让 AI 先读文档再动手，避免"AI 忘了上下文瞎改/把刚修的改回去"（欢迎弹窗重置后反复出现就是回归案例） |

---

## 三、LLM 集成坑速查（现象 → 根因 → 通用对策）

1. **JSON 输出格式不稳定** → 归一化 + 多级解析（parse → 剥代码块 → 截取大括号 → 栈补全）
2. **判断题答案五花八门**（对/正确/true/√…）→ 归一化为「对/错」+ prompt 明确只填两值 + 反例约束
3. **max_tokens 截断 JSON** → 用栈记括号类型逆序补全（{→}、[→]），别用 ] 补所有
4. **LLM 注入任意标签/字段** → 白名单兜底：不在白名单回退默认值
5. **HTTP 200 但 body 是错误/网页** → 解析 body 验证 choices/error/JSON，别只看状态码
6. **空响应（限流）** → 重试 ≤2 次，然后返回已收到的，不 throw 不连坐
7. **先发请求后返回覆盖新状态** → 取号校验（seq）竞态守卫
8. **长流程防重入** → busy 锁，成功/失败都复位
9. **重置/清空后旧数据复活** → reset-ts 时间戳墓碑 + clientTs 校验
10. **数量/配比遵从度低** → 硬底线 + 软配比 + 宁缺毋滥 + 自洽公式
11. **无素材硬凑题** → 没有代码文件就不要求代码题，宁缺毋滥
12. **数据存多处**（localStorage + 服务端文件 + 内存）→ 清空/加检查要穷举所有点；重置顺序：重置内存→清 localStorage→清服务端→空 state 落盘→reload
13. **同类逻辑多路径重复**（面试/理论/实战）→ 修一处 grep 所有路径确保对称
14. **maxTokens 定义了没传进请求**（死变量）→ 定义了的参数确认真的传入调用点
15. **统计口径**：type 与 dimension 是两回事（场景选择题 type=choice、dimension=practical）→ 统计按业务语义字段

---

## 四、附件速查

### A. 提示词五要素检查清单（写/审 prompt 用）
- [ ] 角色：让模型扮演谁（口吻与深浅由它决定）
- [ ] 任务：要它做什么（动词明确）
- [ ] 上下文：给什么材料与约束
- [ ] 输出格式：输出长什么样（JSON schema / 枚举白名单）
- [ ] 示例：正例 + 反例成对（正例教"照做"，反例教"边界在哪"）
> 口诀：「你是谁、干什么、依据什么、输出成什么样、给个样子」

### B. 输出格式约束块模板（附加到每个 LLM 请求尾部）
```
【输出格式要求（程序会严格校验，不满足会要求你重新输出）】
1. 只输出一个 JSON 对象：顶层必须有 <字段> 字段
2. 不要 markdown 代码块，不要任何解释、前后缀、多余文字
3. 每道题严格按模板逐字段填写，字段名一个都不能改
4. 字符串用英文双引号，数组用方括号，数量必须达标，不要少出
5. 实在无法生成时输出 <空值结构>
```

### C. 教学法清单（让题目考"真理解"而非背书）
费曼技巧（去术语化解释）· 苏格拉底追问（层层深入）· 布鲁姆分类（记忆→创造）· 主动回忆（挖空补全）· 类比迁移（生活类比）· 反例与边界（何时失效）

### D. 评审五维度 checklist（大改动后必过）
1. **逻辑正确性**：分支/循环/停止条件，找死循环空转
2. **数据正确性**：字段/去重/统计口径
3. **边界与竞态**：限流/空数据/并发/异步乱序
4. **与现有机制一致性**：其他入口是否同步改（grep 同类路径）
5. **回归风险**：相邻模块是否被误伤；跑测试集 + smoke 主流程

### E. 分阶段开发计划模板
1. 阶段1 数据层+解析（先建测试网）
2. 阶段2 核心业务（纯逻辑，不接 LLM）
3. 阶段3 LLM 集成（统一约束层 + 日志 + 竞态/防重入）
4. 阶段4 打磨（失败保留/自救入口/诊断中心/打包）
> 每阶段可运行、可测试、可回归；改完一步立即验证再进下一步

### F. 会话交接文档（HANDOFF）模板
- 项目现状（最新 commit/测试数/运行方式）
- 架构关键点（每条带文件位置与行号）
- 已知坑（踩过的，防回退）
- 协作模式（评审五维度/用户拍板/提交格式）
- 数据位置 + 日志位置
- 升级史（简短版，回答"你之前改过什么"）
- 遗留/待办

### G. bug 案例映射表（本项目 → 通用场景）
| 本项目现象 | 通用根因 | 通用解法 |
|---|---|---|
| 判断题答案都是第一个 | LLM 输出格式不统一 | 归一化 + prompt 明确 + 反例 |
| 点击下一题无反应 | 按钮状态管理错乱 | 状态机：未答=确认，已答=下一题 |
| 能力分数上来就 100% | 收缩公式参数太松 | 调 BASE/K，测试锁阈值 |
| 多个 toast 重叠 | 每次新建 DOM 节点 | 单例复用 |
| 错误 API 提示"连接成功" | 只判 r.ok 没看 body | 解析 body 验证 |
| 没配 LLM 也能导入出题 | 规则出题绕过 LLM 拦截 | 所有入口统一检查，grep 穷举 |
| 重置后徽章/画像"复活" | fire-and-forget 写盘晚到 | reset-ts 墓碑 + clientTs |
| 不同课程出同一套题 | 只喂概念没喂课程特有素材 | 把特有素材也喂给 LLM + 禁泛化 |
| 实战题统计成理论题 | 按 type 统计而非 dimension | 统计按业务语义字段 |
| 面试考一次≈理论考两次 | 面试权重 ×2 量纲不一致 | 画像统一口径，权重显式审慎 |
| 升级当次徽章不出现 | 状态同步在检查之后 | 先同步再消费 |
| 导入成功提示题数不对 | 用了挂库前数据源 | 用"对时刻"的数据源 |
| 导入成功路径抛 ReferenceError | 变量声明在 try 块内层 | 跨块变量声明提升到外层 |
| LLM 首轮 16 道被截断 | maxTokens 定义了没传 | grep 传参确认 |
| 导入中再次导入并发竞态 | 长流程无防重入锁 | busy 锁 + 全路径复位 |
| 笔记自带测验题混入统计 | 入库来源没分清 | 按 source 区分 LLM 生成 vs 资料自带 |

### H. 测试网设计建议
- 后端：unittest 覆盖纯函数（归一化/公式/阈值/解析），边界值断言锁不变量（如 0/1/3/4→4、5/6/7/8→8）
- 前端：Node vm 加载 JS 文件 + mock 浏览器全局（document/localStorage/fetch/window），测判分/解析/prompt 组装/Logger
- 一键脚本 run_tests.sh 跑全部；每次大改动全量回归
- 覆盖历史 bug（防回退）+ 核心逻辑（不变量）

### I. 打包 / 部署 / 隐私注意事项
- PyInstaller 打包：数据目录放用户主目录私有目录（~/.exam-center），不随包（可写、跨版本持久）
- 打包后静态资源从 _MEIPASS 读，源码运行时用本地 web/——路径双兼容
- 静态资源版本号用文件修改时间戳注入 HTML，根治浏览器缓存旧 JS
- LLM Key 只存浏览器 localStorage，浏览器直连 API，不经服务器
- 本地服务器校验 Host 头防 DNS rebinding；请求体大小上限；文件名白名单防路径穿越

---

## 五、项目级复盘（这个项目做对了什么，可迁移）

1. **先立方法论再写代码**：分工边界（LLM 语义/程序数据）想清楚再动手，避免返工
2. **测试网先行**：重构/大改前先建测试，让测试兜底而不是用户兜底
3. **日志与 LLM 同步上线**：LLM 应用没有日志=瞎子，日志系统不是后期加装而是第一公民
4. **宁缺毋滥贯穿始终**：LLM 的不可靠不是 bug 而是常态，用"软门槛+降级可用+补足入口"拥抱它
5. **用户拍板闭环**：产品规则变更必须人确认，AI 的执行边界清晰
6. **经验持续沉淀**：每次开发/修 bug 后把"现象→根因→解法"写回方法论文档，检查重复与过时
